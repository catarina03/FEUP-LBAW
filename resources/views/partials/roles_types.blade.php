<div class="edit-roles m-0 p-0">
    @if($type == "Regular")
        <button class="btn btn-outline-dark">Regular</button>
    @endif
    @if($type == "Moderator")
        <button class="btn btn-outline-dark">Moderator</button>
    @endif
    @if($type == "System Manager")
        <button class="btn btn-outline-dark">System Manager</button>
    @endif
</div>
